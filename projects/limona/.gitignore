node_modules/
dist/
.env
.env.test
*.log

# Uploaded original documents/assets live here on disk (per document_id /
# asset id) — this is real company content, not something to check into git.
storage/documents/
storage/assets/

# Isolated storage root used by npm run test:db (see .env.test) — keeps DB
# integration test fixtures out of the real production storage/ folder.
storage-test/
