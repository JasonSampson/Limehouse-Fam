import { describe, it, expect } from "vitest";

// Placeholder so `npm test` has something real to run before Q builds the
// actual login/permissions logic this project exists for.
describe("smoke", () => {
  it("runs", () => {
    expect(true).toBe(true);
  });
});
