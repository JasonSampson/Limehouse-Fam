// The Team Performance shared-password functions that used to live here
// (checkTeamPerformancePassword, setTeamPerformancePassword,
// isTeamPerformancePasswordSet) have been removed — real per-person
// Microsoft sign-in (src/db/staffUsers.ts) replaced the shared password
// entirely (see migrations/0007_drop_team_performance_password.ts).
